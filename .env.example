# =============================================================================
# NOVA AI WEB APP - ENVIRONMENT VARIABLES
# =============================================================================
# IMPORTANT: Never commit .env.local to git. All secret keys must stay private.
# Only NEXT_PUBLIC_* variables are safe to expose to the browser.
# =============================================================================

# -----------------------------------------------------------------------------
# AI Integration (SERVER-SIDE ONLY - NEVER expose to browser)
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# Email Integration (SERVER-SIDE ONLY - NEVER expose to browser)
# -----------------------------------------------------------------------------
# Get your key from: https://resend.com/
RESEND_API_KEY=your_resend_api_key_here

# Where NOVA should receive discovery reports
NOVA_REPORTS_EMAIL=founders@your-domain.com

# Optional: custom "from" address for reports (must be verified in Resend)
NOVA_REPORTS_FROM_EMAIL="NOVA Reports <no-reply@your-domain.com>"

# -----------------------------------------------------------------------------
# Public Configuration (SAFE to expose to browser)
# -----------------------------------------------------------------------------
# Public link to your Google Calendar booking / event page
# Example: https://calendar.google.com/calendar/u/0/appointments/schedules/...
# Or Cal.com: https://cal.com/username/discovery
NEXT_PUBLIC_GOOGLE_CALENDAR_LINK=https://calendar.google.com/
